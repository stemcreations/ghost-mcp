# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/

# Secrets
.env

# Tooling caches
.pytest_cache/
.ruff_cache/

# Local-only files (not part of the published package)
# Local MCP client config: holds a machine-specific absolute path to uv/the project
.mcp.json
ghost-llms-full.txt
handoffdoc.md
# Ghost's official "Source" theme, kept locally as a reference for the builder
source/
# Design note; its content now lives in server.py instructions + the theme-a-site prompt
ghost-mcp-theming-guidance.md
landing.html
serenitybook_blog_article_ideas.md
SECURITY_REVIEW.md
CODE_REVIEW.md
.claude/settings.json
logo.svg

# Local research data: the active profile and custom incumbent domains.
# Site-specific, so it stays out of the repo (set GHOST_MCP_DATA_DIR to relocate it).
incumbents.json
.ghost-mcp/

# Business/marketing briefs for the site this MCP manages, not part of the software.
sauna-page-brief.md
